import cv2

class PassInterceptionDrawer:
  def __init__(self):
    pass
  def draw(self, video_frames, passes, interceptions):
    output_video_frames=[]
    for frame_num, frame in enumerate(video_frames):
      frame_drawn=self.draw_frame(frame,fraem_num, passes, interceptions)

  return video_frames

  def 
      
